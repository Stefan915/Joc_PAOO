package StaticObjects;

import Interfaces.I_imageContainer;
import WindowManager.Window;

import java.awt.*;
import java.awt.image.BufferedImage;

public class Exit extends StaticObject implements I_imageContainer {
    public Exit(StaticObjectPosition pos)
    {
        super(pos);
    }

}
